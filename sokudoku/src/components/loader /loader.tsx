import React from 'react';
import './Loader.css'; // Optional: CSS for loader styling

const Loader: React.FC = () => {
  return (
    <div className="loader">
      Loading...
    </div>
  );
};

export default Loader;
